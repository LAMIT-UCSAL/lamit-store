# Story 1.1: Setup do Projeto e Pipeline de Deploy

## Status

Done

## Executor Assignment

executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["coderabbit"]

*(Corrigido por @devops no kickoff: o mapeamento genérico do template — "Infra/CI/CD/Deploy → @devops" — não bate com o conjunto real de comandos deste agente nesta instalação, que é escopado a governança de repositório/push/PR/release, sem comando de implementação de código. @dev executa as Tasks 1-2 (scaffold de arquivos); @devops entra nas Tasks 3-4 pra push/deploy, conforme decisão do usuário.)*

## Story

**As a** mantenedor do projeto,
**I want** ter a estrutura básica do site e um pipeline de deploy automático no Netlify configurados,
**so that** qualquer mudança no repositório vá ao ar de forma confiável e seja possível validar progresso em produção desde o início.

## Acceptance Criteria

1. Repositório contém a estrutura mínima de um site estático (HTML/CSS/JS) rodando localmente sem erros.
2. O projeto está conectado ao Netlify e cada push na branch principal gera um deploy automático.
3. Uma página inicial simples está acessível publicamente via URL do Netlify.

[Source: prd/epic-1-fundacao-catalogo.md#story-11-setup-do-projeto-e-pipeline-de-deploy]

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Deployment/Infrastructure
**Secondary Type(s)**: Nenhum forte — esta story não toca conteúdo/UI (isso é Story 1.2/1.3)
**Complexity**: Low — um arquivo de config (`netlify.toml`) + scaffolding de diretórios, sem dependências externas

### Specialized Agent Assignment

**Primary Agents**:
- @dev (implementa Tasks 1-2: scaffold de arquivos, `netlify.toml`)
- @devops (Tasks 3-4: conecta o repositório ao Netlify, push, valida deploy)

**Supporting Agents**:
- @architect (quality gate desta story, conforme Executor Assignment)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main` antes de criar o pull request
- [ ] Pre-Deployment (@devops): validar `netlify.toml` e confirmar deploy automático funcionando antes de considerar a story "Done" — story é, ela mesma, sobre deployment

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Configuração do pipeline Netlify (`netlify.toml` correto: `publish = "."`, sem comando de build)
- Estrutura de diretórios alinhada ao definido na arquitetura

**Secondary Focus**:
- Nenhum segredo/credencial hardcoded (não deveria haver nenhum nesta story — [Source: architecture/environment-configuration.md])
- Deploy é reversível (rollback nativo do Netlify, sem ação extra necessária)

## Tasks / Subtasks

- [x] Task 1: Criar a estrutura mínima de diretórios do projeto (AC: 1)
  - [x] Criar `index.html`, `produtos/`, `css/`, `js/`, `data/`, `assets/` conforme a árvore definida [Source: architecture/project-structure.md]
  - [x] Criar `css/tokens.css`, `css/base.css`, `css/components.css` como arquivos mínimos/vazios (o conteúdo real vem nas Stories 1.2 e 1.3 — não popular tokens ou componentes aqui)
  - [x] Criar `README.md` com instruções básicas: como rodar localmente (`npx serve .` ou `netlify dev`) [Source: architecture/frontend-developer-standards.md#quick-reference]
  - [x] Confirmar que `index.html` abre localmente sem erros no console do navegador
- [x] Task 2: Configurar `netlify.toml` (AC: 1, 2)
  - [x] Criar `netlify.toml` na raiz do projeto com `publish = "."` e `command = ""` (sem build step) [Source: architecture/environment-configuration.md]
- [x] Task 3: Conectar o repositório ao Netlify e validar deploy automático (AC: 2, 3)
  - [x] Ação humana: conta Netlify conectada ao repositório pelo usuário — ver User Actions [Source: prd/next-steps.md#user-actions-tarefas-s-humanas]
  - [x] Push feito na branch principal (`origin/main`, commit `6872e22`) — Netlify buildou e publicou automaticamente
  - [x] Confirmado: URL pública `https://lamitstore.netlify.app` carrega a página inicial sem erro
- [x] Task 4: Validação manual (AC: 1, 2, 3)
  - [x] Rodado localmente e confirmado ausência de erros (servidor de teste, todos os assets 200) [Source: architecture/testing-requirements.md]
  - [x] URL pública do Netlify verificada via curl: `/` (200) e os 4 assets referenciados (`tokens.css`, `base.css`, `components.css`, `main.js`) todos 200

## Dev Notes

**Previous Story Insights:** N/A — esta é a primeira story do projeto, não há story anterior.

**Data Models:** N/A para esta story. `data/products.json` é criado na Story 1.3, não aqui — esta story só garante que a pasta `data/` existe. [Source: architecture/project-structure.md]

**API Specifications:** N/A — o projeto não tem API própria; toda integração externa (Google Forms, InfinitePay) é só link, sem chamada de API do site. [Source: architecture/api-integration.md]

**Component Specifications:** N/A para esta story — nenhum componente visual é implementado aqui (isso é Story 1.2 para tokens/identidade visual e Story 1.3 para o grid de catálogo).

**File Locations:** estrutura completa de diretórios a criar:

```plaintext
lamit-store/
├── index.html
├── produtos/
├── css/
│   ├── tokens.css
│   ├── base.css
│   └── components.css
├── js/
│   └── main.js
├── data/
├── assets/            # já existe (logos + fotos de produto já fornecidos)
├── netlify.toml
└── README.md
```
[Source: architecture/project-structure.md]

**Technical Constraints:**
- Nenhum framework, nenhum bundler — HTML/CSS/JS vanilla. [Source: architecture/frontend-tech-stack.md]
- Nenhuma variável de ambiente necessária nesta ou em nenhuma story do projeto. [Source: architecture/environment-configuration.md]
- Regra crítica aplicável mesmo nesta story preliminar: nunca introduzir framework JS "pra facilitar" — quebra a decisão de stack já confirmada em 3 documentos anteriores. [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 6]
- Convenção de nomes de arquivo: kebab-case. [Source: architecture/frontend-developer-standards.md#quick-reference]

**Project Structure Notes:** nenhum conflito encontrado — esta story implementa a estrutura definida na arquitetura pela primeira vez, não há estrutura pré-existente para reconciliar.

### Testing

- **Test file location:** N/A — sem testes automatizados neste projeto.
- **Test standards:** validação manual conforme checklist. [Source: architecture/testing-requirements.md]
- **Testing frameworks:** nenhum.
- **Requisitos específicos desta story:** confirmar ausência de erros no console ao rodar localmente (AC1); confirmar que o deploy automático do Netlify funciona após push (AC2); confirmar que a URL pública carrega a página inicial sem erro (AC3). Não há mais nada a testar nesta story — conteúdo de marca e catálogo são cobertos pelas Stories 1.2 e 1.3.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-13 | 0.1.0 | Story criada a partir de `prd/epic-1-fundacao-catalogo.md` | River (@sm) |
| 2026-08-13 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-08-13 | 0.1.2 | Corrigido Executor Assignment (devops → dev) e config de self-healing — mapeamento genérico do template não bate com o escopo real do agente @devops nesta instalação | Gage (@devops) |
| 2026-08-13 | 0.2.0 | Tasks 1-2 implementadas (scaffold + netlify.toml). Status: Ready → InProgress | Dex (@dev) |
| 2026-08-13 | 0.3.0 | Tasks 3-4 implementadas: push pra origin/main, deploy Netlify confirmado em `lamitstore.netlify.app`. Status: InProgress → Review | Gage (@devops) |
| 2026-08-13 | 1.0.0 | QA Gate: PASS — todas as ACs verificadas independentemente. Status: Review → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev persona "Dex", via AIOX)

### Debug Log References

Nenhum erro encontrado. Validação local via `npx serve . -l 5050`: `/` (200), `/css/tokens.css` (200), `/css/base.css` (200), `/css/components.css` (200), `/js/main.js` (200). `index.html` retorna 301 quando acessado por esse path exato (redirect do `serve` pra `/`, comportamento padrão de `cleanUrls` — não é erro).

### Completion Notes List

- Tasks 1 e 2 completas: estrutura de diretórios, arquivos CSS/JS mínimos (populados nas Stories 1.2/1.3, conforme instruído), `README.md`, `netlify.toml`.
- `index.html` é uma página canary mínima — conteúdo real do catálogo vem na Story 1.3 (AC1 desta story só exige "roda localmente sem erros", não o design final).
- Tasks 3-4 (conectar Netlify, push, validar deploy público) ficam para `@devops`, conforme divisão de trabalho já registrada no Executor Assignment.
- Nenhuma dependência nova, nenhuma decisão técnica fora do que já estava documentado nas Dev Notes.
- **@devops (Gage):** Tasks 3-4 concluídas. Conta Netlify já estava sendo conectada pelo usuário durante a story. Pre-push quality gate: CodeRabbit indisponível neste ambiente (nem nativo nem via WSL — timeout), demais checks (lint/test/typecheck/build) N/A por não haver tooling Node no projeto. Commit `6872e22` enviado a `origin/main` com confirmação do usuário. Deploy automático confirmado: `https://lamitstore.netlify.app` retorna 200 com o conteúdo correto, e os 4 assets referenciados carregam sem erro 404. URL real (`lamitstore.netlify.app`, sem hífen) corrigida em `docs/prd.md` e `docs/prd/next-steps.md`, que tinham um exemplo hifenizado especulativo.

### File List

- `index.html` (criado)
- `css/tokens.css` (criado, mínimo)
- `css/base.css` (criado, mínimo)
- `css/components.css` (criado, mínimo)
- `js/main.js` (criado, mínimo)
- `produtos/` (criado, vazio)
- `data/` (criado, vazio)
- `netlify.toml` (criado)
- `README.md` (criado)

## QA Results

### Review Date: 2026-08-13

### Reviewed By: Quinn (Test Architect)

### Reviewed Revision: 997619fee51b7132fa61d7cde2439158148721c4

### Code Quality Assessment

Implementação enxuta e fiel ao escopo da story — scaffold de diretórios, `index.html` canary mínimo, `netlify.toml` correto, CSS/JS deliberadamente vazios (com comentário indicando onde serão populados). Nenhum excesso, nenhuma lacuna. Verifiquei a árvore de arquivos localmente e o deploy em produção de forma independente (não confiei apenas nos relatos de @dev/@devops): `https://lamitstore.netlify.app/` → 200 com o HTML exato do commit; os 4 assets referenciados → 200; uma rota inexistente → 404 (comportamento correto, sem fallback silencioso).

### Refactoring Performed

Nenhuma — não há nada a refatorar em arquivos placeholder mínimos e corretos.

### Compliance Check

- Coding Standards: ✓ — nenhuma regra de `architecture/frontend-developer-standards.md` violada (sem cores hardcoded, sem framework introduzido, sem CTA quebrado — nenhum desses elementos existe ainda nesta story, corretamente)
- Project Structure: ✓ — árvore idêntica à `architecture/project-structure.md`
- Testing Strategy: ✓ — sem testes automatizados por decisão documentada; validação manual executada e evidenciada (curl local + produção)
- All ACs Met: ✓ — AC1 (roda localmente sem erro), AC2 (deploy automático confirmado por 2 pushes consecutivos), AC3 (URL pública `lamitstore.netlify.app` acessível) — todos re-verificados por mim, não só aceitos do relato do dev

### Improvements Checklist

- [x] Verificação independente do deploy em produção (root + assets + rota 404)
- [ ] (Nice-to-have, não bloqueia) `data/` e `produtos/` estão vazios e o Git não rastreia diretórios vazios — sem risco prático, já que as Stories 1.2/1.3 populam esses diretórios em seguida, mas vale um `.gitkeep` se algum dia precisar clonar o repo nesse meio-tempo exato.

### Security Review

Nenhum problema. Sem segredos no código, `.env` corretamente no `.gitignore` e não commitado, `netlify.toml` sem credenciais.

### Performance Considerations

Nenhum problema — arquivos estáticos triviais (poucos KB no total).

### Files Modified During Review

Nenhum arquivo de implementação foi modificado durante a revisão.

### Gate Status

Gate: PASS → docs/qa/gates/1.1-setup-projeto-pipeline-deploy.yml

*(Risk profile e NFR assessment completos não foram gerados como arquivos separados — risco avaliado como baixo no Passo 1, escopo é scaffolding sem lógica de aplicação; gerar esses artefatos seria over-engineering para esta story.)*

### Lifecycle Transition

PASS: InReview → Done
