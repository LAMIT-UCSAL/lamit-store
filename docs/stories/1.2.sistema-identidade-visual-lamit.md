# Story 1.2: Sistema de Identidade Visual LAMIT

## Status

Done

## Executor Assignment

executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["coderabbit"]

*(Design/UI Components → executor @dev nesta instalação, já que @ux-design-expert não está no ciclo de execução de código; @architect segue como quality gate.)*

## Story

**As a** desenvolvedor do site,
**I want** ter os tokens de marca da LAMIT (cores, tipografia, componentes visuais base) implementados de forma reutilizável,
**so that** todas as páginas apliquem a identidade oficial de forma consistente, sem reinventar estilo página a página.

## Acceptance Criteria

1. Paleta oficial (azul institucional `#0A69C4`, laranja `#E8722C`, navy `#0E1B27`, cream `#FBFAF8`) definida como variáveis CSS reutilizáveis.
2. Logo(s) da LAMIT (a partir de `assets/`) integrados e disponíveis para uso nas páginas.
3. Estilo base de tipografia e componentes (botões, cards) reflete a linguagem gráfica oficial (scrapbook/editorial/artesanal) do guia de marca.

[Source: prd/epic-1-fundacao-catalogo.md#story-12-sistema-de-identidade-visual-lamit]

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: Nenhum forte
**Complexity**: Low — só CSS + reorganização de assets estáticos, sem lógica

### Specialized Agent Assignment

**Primary Agents**:
- @dev (implementa)

**Supporting Agents**:
- @architect (quality gate)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main` antes do push

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Acessibilidade: contraste de cor (atenção ao laranja `#E8722C` em texto pequeno — ver Dev Notes)
- Responsividade: mobile-first (NFR1)

**Secondary Focus**:
- Nenhuma cor hardcoded fora de `tokens.css`
- `prefers-reduced-motion` respeitado em qualquer transição adicionada

## Tasks / Subtasks

- [x] Task 1: Popular `css/tokens.css` com a paleta oficial e variáveis auxiliares (AC: 1)
  - [x] Copiar o bloco de CSS Custom Properties definido em [Source: architecture/styling-guidelines.md#global-theme-variables] — cores reais da marca + tipografia/espaçamento marcados como placeholder no próprio comentário do código
  - [x] Incluir o `@media (prefers-reduced-motion: reduce)` já especificado na mesma fonte
- [x] Task 2: Organizar e integrar os logos da LAMIT (AC: 2)
  - [x] Mover `assets/01_LAMIT_HORIZONTAL.png`, `02_LAMIT_VERTICAL.png`, `03_LAMIT_ESCUDO.png`, `lamitEscudoVazadoBranco.png`, `lamitEscudoVazadoAzul.svg` para `assets/logos/`, conforme [Source: architecture/project-structure.md]
  - [x] Atualizar `index.html` para usar o logo horizontal no header (`assets/logos/01_LAMIT_HORIZONTAL.png`) com `alt` descritivo (ex: "Logo LAMIT")
  - [x] Confirmar que nenhuma referência quebrada resulta da movimentação (nenhum outro arquivo do projeto referenciava os caminhos antigos até esta story)
- [x] Task 3: Popular `css/base.css` com reset e estilos globais (AC: 3)
  - [x] Reset básico (box-sizing, margens zeradas em elementos padrão)
  - [x] Aplicar `var(--color-bg)` no `body`, `var(--color-text)` no texto, `var(--font-primary)` como fonte base [Source: architecture/styling-guidelines.md#global-theme-variables]
  - [x] Hierarquia de headings usando `--font-size-h1`/`--font-size-h2`/`--font-size-body`/`--font-size-small` já definidos nos tokens
- [x] Task 4: Popular `css/components.css` com estilo base de botão/CTA e de card (AC: 3)
  - [x] Estilo visual base de um botão/CTA reutilizável — usa `var(--color-accent)`, reflete a linguagem gráfica scrapbook/editorial (leve rotação/sombra, não um botão corporativo genérico). Uso real (CTA "Pedir") vem no Epic 2 — aqui é só o estilo base.
  - [x] Estilo visual base de "card" reutilizável, seguindo o Component Template já documentado: leve elevação/rotação no hover/focus (`translateY(-4px) rotate(-0.5deg)`) [Source: architecture/component-standards.md#component-template]. Uso real (Product Card) vem na Story 1.3.
- [x] Task 5: Validação manual (AC: 1, 2, 3)
  - [x] Rodado localmente e confirmado: cores/tipografia dos tokens aplicadas, logo carrega, sem erros (servidor de teste, todos os recursos 200)
  - [x] Checagem de contraste do laranja de accent: `--color-bg` sobre `--color-accent` deu ~2.93:1 (falha) — corrigido pra `--color-secondary` sobre `--color-accent`, ~5.71:1 (passa AA). Cálculo manual via fórmula de luminância relativa do WCAG (sem Lighthouse/axe disponíveis neste ambiente).

## Dev Notes

**Previous Story Insights:** Story 1.1 criou `css/tokens.css`, `css/base.css`, `css/components.css` como arquivos mínimos (só um comentário cada), deliberadamente vazios pra esta story popular. `index.html` já existe com a ordem de import correta (`tokens.css` → `base.css` → `components.css` → `main.js`) — não precisa recriar, só editar. [Source: stories/1.1.setup-projeto-pipeline-deploy.md]

**Data Models:** N/A para esta story.

**API Specifications:** N/A — projeto não tem API própria.

**Component Specifications:**
- Botão/CTA: variante única (não há CTAs secundários no site), estado normal + opcional desabilitado/aguardando [Source: front-end-spec.md#cta-button-pedir] — nesta story, só o estilo visual base, sem o link real do Forms (isso é Epic 2).
- Card: usado como base do Product Card da Story 1.3; template de marcação de referência já existe [Source: architecture/component-standards.md#component-template].

**File Locations:**
- `assets/logos/` é um diretório novo (ainda não existe) — os 5 arquivos de logo estão hoje soltos na raiz de `assets/` [verificado via listagem direta do diretório nesta sessão].
- `css/tokens.css`, `css/base.css`, `css/components.css` já existem (Story 1.1), só precisam de conteúdo.

**Technical Constraints:**
- Paleta oficial é vinculante — não inventar novas cores. Sucesso/aviso/erro ficam `N/A` porque o guia de marca não define essas cores e o site não tem estados que as produzam [Source: front-end-spec.md#color-palette].
- Tipografia e espaçamento numérico NÃO são definidos pelo guia de marca — os valores em `tokens.css` são placeholders explícitos (já com comentário no próprio código-fonte), não uma decisão final de marca [Source: architecture/styling-guidelines.md#global-theme-variables].
- Nunca hardcode cores fora de `tokens.css` [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 1].
- Toda imagem (incluindo o logo) exige `alt` descritivo [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 4].
- Respeitar `prefers-reduced-motion` em qualquer transição/animação [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 5].

**Project Structure Notes:** cria `assets/logos/` pela primeira vez, alinhado à árvore já definida na arquitetura — sem conflito.

### Testing

- **Test file location:** N/A — sem testes automatizados neste projeto.
- **Test standards:** validação manual conforme checklist. [Source: architecture/testing-requirements.md]
- **Testing frameworks:** nenhum.
- **Requisitos específicos desta story:** confirmar visualmente que cores/tipografia dos tokens são aplicadas; confirmar que o logo carrega sem 404 após a movimentação de arquivo; checar contraste do laranja de accent com uma ferramenta (Lighthouse/axe) antes de considerar a story completa.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-13 | 0.1.0 | Story criada a partir de `prd/epic-1-fundacao-catalogo.md` | River (@sm) |
| 2026-08-13 | 0.2.0 | Todas as tasks implementadas, incluindo correção de contraste no botão. Status: Draft → Review (sem passar por @po, por instrução do usuário) | Dex (@dev) |
| 2026-08-13 | 1.0.0 | QA Gate: PASS — contraste reverificado independentemente, ACs confirmadas. Status: Review → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev persona "Dex", via AIOX)

### Debug Log References

Nenhum erro. Validação local via `npx serve . -l 5051`: `/` (200), `tokens.css`/`base.css`/`components.css` (200), `assets/logos/01_LAMIT_HORIZONTAL.png` (200).

### Completion Notes List

- Tokens populados com a paleta real da marca; tipografia/espaçamento marcados como placeholder explícito no código (não há definição no guia de marca).
- Logos movidos de `assets/` (raiz) para `assets/logos/`, conforme arquitetura. Nenhuma referência quebrada — verificado via busca no código antes e depois da movimentação.
- **Achado de acessibilidade (corrigido nesta story):** o par `--color-bg` (texto) sobre `--color-accent` (fundo) no botão base tinha contraste ~2.93:1, abaixo do mínimo de 3:1 do WCAG AA mesmo pra texto grande. Troquei o texto do botão pra `--color-secondary` (navy) — novo contraste ~5.71:1, passa AA confortavelmente. Cálculo feito manualmente com a fórmula de luminância relativa do WCAG, já que Lighthouse/axe não estão disponíveis neste ambiente.
- Botão/CTA e card são só a base visual nesta story — sem uso real ainda (isso vem nas Stories 1.3 e no Epic 2).
- Nenhuma dependência nova.

### File List

- `css/tokens.css` (modificado — populado)
- `css/base.css` (modificado — populado)
- `css/components.css` (modificado — populado)
- `index.html` (modificado — header com logo)
- `PRODUCT.md` (modificado — corrigido caminho dos logos)
- `assets/logos/01_LAMIT_HORIZONTAL.png` (movido de `assets/`)
- `assets/logos/02_LAMIT_VERTICAL.png` (movido de `assets/`)
- `assets/logos/03_LAMIT_ESCUDO.png` (movido de `assets/`)
- `assets/logos/lamitEscudoVazadoBranco.png` (movido de `assets/`)
- `assets/logos/lamitEscudoVazadoAzul.svg` (movido de `assets/`)

## QA Results

### Review Date: 2026-08-13

### Reviewed By: Quinn (Test Architect)

### Reviewed Revision: working tree (pré-commit) — arquivos revisados diretamente: `css/tokens.css`, `css/base.css`, `css/components.css`, `index.html`, `assets/logos/*`

### Code Quality Assessment

Implementação correta e proporcional ao escopo. Destaque positivo: o dev não só implementou como encontrou e corrigiu um problema real de acessibilidade (contraste do botão) durante a própria validação da story, em vez de deixar pra descobrir depois. Reverifiquei o cálculo de forma independente (script Node com a fórmula oficial de luminância relativa do WCAG, não confiei no relato) — os números batem exatamente: `2.93:1` (falha) → `5.71:1` (passa AA) na correção. De brinde, chequei os outros dois pares de cor já em uso (texto/fundo: `16.71:1`; link/fundo: `5.26:1`) — ambos com folga confortável.

### Refactoring Performed

Nenhuma — nada a melhorar no que foi entregue.

### Compliance Check

- Coding Standards: ✓ — nenhuma cor hardcoded fora de `tokens.css` (verificado via busca em `base.css`/`components.css`, zero ocorrências de hex)
- Project Structure: ✓ — `assets/logos/` criado conforme arquitetura, nenhuma referência quebrada pela movimentação dos logos
- Testing Strategy: ✓ — validação manual executada e evidenciada; achado de acessibilidade tratado como parte do processo, não ignorado
- All ACs Met: ✓ — AC1 (paleta como CSS vars, confirmado por leitura direta de `tokens.css`), AC2 (logo integrado e visível no header, `alt` presente), AC3 (base.css/components.css refletem a linguagem gráfica com rotação sutil nos hovers, dentro do que é razoável pra uma story de fundação — texturas/scrapbook completas vêm depois)

### Improvements Checklist

- [x] Verificação independente do cálculo de contraste (script Node, fórmula WCAG)
- [x] Verificação de que nenhuma cor está hardcoded fora de tokens.css
- [ ] (Nice-to-have) Quando o Epic 2 implementar o CTA "Pedir" de verdade, revalidar contraste se a cor de texto do botão mudar por qualquer motivo

### Security Review

Nenhum problema — apenas CSS e reorganização de assets estáticos.

### Performance Considerations

Nenhum problema — sem mudança de peso relevante.

### Files Modified During Review

Nenhum arquivo de implementação foi modificado durante a revisão.

### Gate Status

Gate: PASS → docs/qa/gates/1.2-sistema-identidade-visual-lamit.yml

### Lifecycle Transition

PASS: InReview → Done
