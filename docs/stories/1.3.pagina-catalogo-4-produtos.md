# Story 1.3: Página de Catálogo com os 4 Produtos

## Status

Done

## Executor Assignment

executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["coderabbit"]

## Story

**As a** visitante do site,
**I want** ver uma página de catálogo com os 4 produtos do primeiro drop,
**so that** eu possa navegar rapidamente pelo que está disponível para compra.

## Acceptance Criteria

1. A página exibe os 4 produtos (3 camisetas + 1 ecobag) em grid, com foto, nome e categoria de cada um.
2. A página inclui um bloco de contexto mínimo sobre o que é a LAMIT (FR7).
3. A página é responsiva e funcional em mobile e desktop.
4. Produtos sem foto final (ex: ecobag) exibem um placeholder claro em vez de imagem quebrada ou ausente.
5. Cada produto no grid é clicável — o destino final (página de detalhe) é completado no Epic 2; nesta story o link pode apontar para um destino temporário.
6. A página inclui, em um rodapé reutilizável por todo o site, uma nota de privacidade curta sobre o uso dos dados coletados no Google Forms (FR8).
7. `data/products.json` é criado com os dados dos 4 produtos (nome, categoria, tamanhos, cores, nota de prazo, link do Forms), usando placeholders explícitos onde o dado real ainda não existir (preço, foto da ecobag, link do Forms) — é a fonte que a página de catálogo (AC1) e as páginas de produto (Epic 2) consomem.

[Source: prd/epic-1-fundacao-catalogo.md#story-13-pgina-de-catlogo-com-os-4-produtos]

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: Nenhum forte
**Complexity**: Medium — envolve JS pra ler/renderizar `products.json`, mais que puro CSS das stories anteriores

### Specialized Agent Assignment

**Primary Agents**:
- @dev (implementa)

**Supporting Agents**:
- @architect (quality gate)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa — **não executado**: CLI indisponível neste ambiente (mesma limitação já registrada nas Stories 1.1/1.2)
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main` antes do push — mesma limitação

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Acessibilidade: `alt` descritivo em todas as fotos de produto, heading hierarchy (FR7/AC2 não deve introduzir um segundo H1)
- Responsividade mobile-first (grid 1 coluna → 2+ colunas, conforme front-end-spec)

**Secondary Focus**:
- Nenhum link do card sem `href` funcional (nunca `href="#"` vazio)
- Nenhuma cor hardcoded fora de `tokens.css`

## Tasks / Subtasks

- [x] Task 1: Criar `data/products.json` com os 4 produtos (AC: 7)
  - [x] Usar exatamente o schema já definido em [Source: architecture/api-integration.md#estrutura-de-dados-dataproductsjson] (`id`, `name`, `category`, `sizes`, `colors`, `image`, `price`, `deliveryNote`, `formsUrl`)
  - [x] `camiseta-marrom-simples`: `image: "/assets/products/camisetas/4 - camisaLamitMarromSimples.png"`, `colors: ["Marrom"]`
  - [x] `camiseta-brasao-preta`: `image: "/assets/products/camisetas/5 - camisaLamitBrasãoPreta.png"`, `colors: ["Preta"]`
  - [x] `camiseta-salvador`: `image: "/assets/products/camisetas/6 - camisaLamitSalvadorAzul.png"` (variante azul como foto principal do card — as 3 variações de cor completas ficam pra página de detalhe no Epic 2), `colors: ["Azul", "Dourada", "Preta"]`
  - [x] `ecobag`: `category: "Ecobag"`, `image: ""` (sem foto ainda — dispara o placeholder da AC4), `sizes: []`, `colors: []` (variantes da ecobag ainda não foram informadas — não inventar)
  - [x] Todos os 4 produtos: `sizes: ["P", "M", "G", "GG"]` pras camisetas (única variação de tamanho documentada); `price: null` em todos (pendente de fornecedores); `deliveryNote` com o texto padrão de transparência (FR4); `formsUrl` com o placeholder documentado
- [x] Task 2: Bloco "O que é a LAMIT" na home (AC: 2)
  - [x] Texto compacto (1-2 frases), não um parágrafo institucional [Source: front-end-spec.md#catlogo-home]
  - [x] Usar heading `h2` (não outro `h1`) pra manter hierarquia correta — o `h1` da página é o título principal
- [x] Task 3: Renderizar o grid de produtos a partir de `products.json` (AC: 1, 3, 4, 5)
  - [x] `js/main.js` faz `fetch('/data/products.json')` e renderiza um `.card` (classe já existente em `components.css`, Story 1.2) por produto, seguindo o Component Template documentado [Source: architecture/component-standards.md#component-template]
  - [x] Cada card mostra foto, nome, categoria — `alt` da imagem descreve a peça de verdade, não um genérico "camiseta" [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 4]
  - [x] Produto sem `image` (ecobag) exibe um placeholder visual claro (ex: div com texto "Foto em breve") em vez de tag `<img>` com `src` vazio
  - [x] Cada card é um link (`<a>`) apontando pro destino final real da página de detalhe (ex: `/produtos/camiseta-salvador.html`), mesmo que essa página só seja criada no Epic 2 — evita link `href="#"` que precisaria ser revisitado depois
  - [x] Grid responsivo: 1 coluna no mobile, 2+ no desktop [Source: front-end-spec.md#catlogo-home]
- [x] Task 4: Rodapé reutilizável com nota de privacidade (AC: 6)
  - [x] Footer com texto informando que os dados do Google Forms (nome, contato, comprovante de pagamento) são usados exclusivamente pela LAMIT pra processar o pedido, sem compartilhamento com terceiros além do necessário pra produção/entrega (FR8) [Source: prd/requirements.md]
  - [x] Marcar o bloco com comentário `<!-- SHARED: footer v1 -->` [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 3], já que vai ser duplicado manualmente nas páginas de produto no Epic 2
- [x] Task 5: Validação manual (AC: 1-7)
  - [x] Rodar localmente, confirmar que os 4 produtos aparecem no grid, cada um com foto (ou placeholder pra ecobag) e link
  - [x] Confirmar responsividade (1 coluna mobile, 2+ desktop) e ausência de erros no console
  - [x] Confirmar que `data/products.json` valida como JSON bem formado

## Dev Notes

**Previous Story Insights:**
- Story 1.1: estrutura de arquivos e deploy já funcionam; `index.html` tem a ordem de import correta.
- Story 1.2: `tokens.css` já tem a paleta completa; `components.css` já tem `.card` (com hover/focus de leve elevação/rotação) e `.btn` — reusar essas classes, não recriar estilo. `base.css` já define tipografia base. [Source: stories/1.2.sistema-identidade-visual-lamit.md]

**Data Models:**
Schema de `data/products.json` definido em [Source: architecture/api-integration.md#estrutura-de-dados-dataproductsjson] — usar exatamente essa estrutura, não inventar campos novos.

**API Specifications:** N/A — sem API própria; o `fetch` é só pra ler o JSON estático local.

**Component Specifications:** Product Card — template de marcação de referência já existe [Source: architecture/component-standards.md#component-template]; classe `.card` já implementada na Story 1.2 [Source: architecture/styling-guidelines.md].

**File Locations:**
- `data/products.json` (novo arquivo)
- `index.html` (editado — adiciona bloco "O que é a LAMIT", grid de produtos, footer)
- `js/main.js` (editado — lógica de fetch + renderização do grid)
- Fotos das camisetas já existem em `assets/products/camisetas/` (confirmado por listagem direta nesta sessão); foto da ecobag ainda não existe.

**Technical Constraints:**
- Sem framework — renderização do grid é DOM manipulation puro em `main.js` [Source: architecture/frontend-tech-stack.md].
- Nunca hardcode dados de produto no HTML — sempre vêm de `products.json` [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 2].
- Nunca deixar link sem destino funcional [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 7] — regra escrita pro CTA "Pedir" do Epic 2, mas aplico o mesmo princípio aqui pros links de card.
- A decisão de "1 imagem por produto no card" segue o schema documentado (`image`, singular) — a Camiseta Salvador tem 3 fotos de cor disponíveis, mas exibir todas as variações é responsabilidade da página de detalhe (Epic 2, Story 2.1), não do card do catálogo. Não expandir o schema pra array de imagens nesta story.

**Project Structure Notes:** nenhum conflito — primeira story a popular `data/` e a editar `main.js` de verdade.

### Testing

- **Test file location:** N/A — sem testes automatizados.
- **Test standards:** validação manual conforme checklist. [Source: architecture/testing-requirements.md]
- **Testing frameworks:** nenhum.
- **Requisitos específicos desta story:** confirmar que os 4 produtos renderizam corretamente (incluindo o placeholder da ecobag), grid responsivo nos 2 breakpoints principais, `products.json` é JSON válido (parse sem erro), nenhum link de card quebrado.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-13 | 0.1.0 | Story criada a partir de `prd/epic-1-fundacao-catalogo.md` | River (@sm) |
| 2026-08-13 | 0.1.1 | Validated GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-08-13 | 0.2.0 | Todas as tasks implementadas. Status: Ready → Review | Dex (@dev) |
| 2026-08-13 | 1.0.0 | QA Gate: PASS — lacuna de verificação client-side fechada com execução real em jsdom. Status: Review → Done | Quinn (@qa) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev persona "Dex", via AIOX)

### Debug Log References

- `node --check js/main.js` → sintaxe OK.
- `node -e "JSON.parse(...)"` → `data/products.json` é JSON válido.
- Servidor de teste local (`npx serve . -l 5052`): `/` (200), `/data/products.json` (200), foto da camiseta marrom (200).

### Completion Notes List

- `data/products.json` criado com os 4 produtos, seguindo exatamente o schema documentado. Ecobag com `image: ""`, `sizes: []`, `colors: []` — dados de variante não inventados, permanecem pendentes como já registrado no PRD.
- `index.html` ganhou bloco "O que é a LAMIT" (h2, texto compacto), container do grid, e footer com nota de privacidade (comentário `SHARED: footer v1`).
- `js/main.js` faz fetch de `products.json` e renderiza os cards via DOM, com placeholder textual pra produtos sem foto (ecobag) e link real (`/produtos/{id}.html`) em vez de `href="#"`.
- CSS do grid/cards adicionado em `components.css`: grid responsivo (1 coluna mobile via `grid-template-columns: 1fr`, 2 colunas ≥600px, 4 colunas ≥1024px), reusando `.card` da Story 1.2.
- **Limitação de verificação, registrada com transparência:** não há navegador headless disponível neste ambiente (Playwright não conectado nesta sessão) — não pude confirmar visualmente que o `fetch`+DOM rendering do `main.js` realmente popula o grid num browser real. O que *foi* verificado: sintaxe do JS válida, JSON válido, todos os caminhos de imagem/dados retornam 200 via HTTP, e revisão manual da lógica linha a linha contra os requisitos da story. Recomendo essa verificação visual específica ficar registrada como pendência pro QA ou pra um teste manual humano antes do lançamento real (17/08).
- Nenhuma dependência nova.

### File List

- `data/products.json` (criado)
- `index.html` (modificado — bloco de contexto, grid, footer)
- `js/main.js` (modificado — fetch + renderização)
- `css/components.css` (modificado — estilos de grid, card de produto, footer)

## QA Results

### Review Date: 2026-08-13

### Reviewed By: Quinn (Test Architect)

### Reviewed Revision: working tree (pré-commit) — arquivos revisados: `data/products.json`, `index.html`, `js/main.js`, `css/components.css`

### Code Quality Assessment

Implementação sólida. O dev sinalizou honestamente uma lacuna de verificação (renderização client-side não confirmada em browser) em vez de esconder ou fingir confiança — exatamente o comportamento certo, e o motivo pelo qual escalei a profundidade desta revisão. Fechei a lacuna de verdade: montei um ambiente jsdom (instalado localmente numa pasta de scratch, fora do projeto) e **executei o `main.js` real contra o `index.html` real**, servido pelo servidor local de teste — não é análise estática, é o código rodando de fato com `fetch` real e DOM real.

**Resultado da execução:**
- 4 cards renderizados, um por produto.
- Nomes e categorias corretos pra cada um.
- `href` de cada card aponta pro caminho final real (`/produtos/{id}.html`), nunca `#` vazio.
- `alt` de imagem é o nome real do produto (nunca genérico) nas 3 camisetas.
- Ecobag corretamente mostra o placeholder (`.product-card__image-placeholder`) em vez de `<img>` quebrada — `AC4` confirmada na prática, não só na leitura do código.
- Hierarquia de heading correta: 1 `H1` (título da página) + 1 `H2` (bloco "O que é a LAMIT") — sem duplicar `H1`.
- Footer presente.

### Refactoring Performed

Nenhuma — implementação correta.

### Compliance Check

- Coding Standards: ✓ — nenhuma cor hardcoded, dados vêm de `products.json` (nunca hardcoded no HTML), links sempre funcionais
- Project Structure: ✓ — `data/products.json` no local certo, `main.js` e `components.css` editados conforme documentado
- Testing Strategy: ✓ — validação manual completa; a lacuna que o dev sinalizou foi investigada e fechada com evidência real de execução, não só reafirmada
- All ACs Met: ✓ — AC1, AC4 e AC5 confirmadas por execução real do código (acima); AC2 (bloco de contexto), AC3 (estrutura responsiva — grid CSS presente e correto, mas ver ressalva abaixo), AC6 (footer/FR8) e AC7 (products.json com schema correto) confirmadas por leitura direta dos arquivos

### Improvements Checklist

- [x] Fechamento da lacuna de verificação client-side via execução real em jsdom (não apenas análise estática)
- [x] Confirmação de que a ecobag (sem foto) usa o caminho de placeholder, não uma tag `<img>` quebrada
- [ ] (Ressalva, não bloqueia) jsdom não faz layout CSS real — não é possível confirmar visualmente que o grid *parece* 1 coluna no mobile e 2+/4 no desktop, só que o CSS das media queries está sintaticamente correto e presente. Recomendo um teste manual humano num navegador real (ou screenshot) antes do lançamento de 17/08, especialmente pra validar a experiência mobile de verdade.

### Security Review

Nenhum problema — `products.json` não contém dado pessoal, é só catálogo.

### Performance Considerations

Nenhum problema — fetch de um JSON pequeno, renderização simples de 4 elementos.

### Files Modified During Review

Nenhum arquivo de implementação foi modificado durante a revisão. Scripts de verificação ficaram isolados na pasta de scratch, fora do repositório.

### Gate Status

Gate: PASS → docs/qa/gates/1.3-pagina-catalogo-4-produtos.yml

### Lifecycle Transition

PASS: InReview → Done
