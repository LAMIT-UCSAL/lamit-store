# Story 2.1: Página de Detalhe de Produto

## Status

Review

## Executor Assignment

executor: "dev"
quality_gate: "architect"
quality_gate_tools: ["coderabbit"]

## Story

**As a** visitante do site,
**I want** ver uma página de detalhe completa ao clicar em um produto do catálogo,
**so that** eu possa conhecer fotos, descrição, tamanhos e cores antes de decidir comprar.

## Acceptance Criteria

1. Cada um dos 4 produtos tem sua própria página de detalhe, acessível a partir do card correspondente no catálogo (FR2).
2. A página exibe fotos do produto (ou placeholder claro se ainda não disponível — caso da ecobag), descrição, tamanhos e cores disponíveis (FR3).
3. A página segue a identidade visual definida na Story 1.2.
4. A página é responsiva em mobile e desktop.

[Source: prd/epic-2-paginas-de-produto-encaminhamento-de-pedido.md#story-21-pgina-de-detalhe-de-produto]

**Nota de escopo (@sm):** o schema de `data/products.json` (Story 1.3) não tem campo de descrição, embora FR3 exija um. Também faltava suporte a múltiplas fotos por produto — necessário porque a Camiseta Salvador tem 3 variações reais de cor de camiseta/estampa (verificado por inspeção direta das imagens em `assets/products/camisetas/`, não são a mesma foto repetida). Task 1 desta story estende o schema (`image` singular → `images` array, + campo `description`) e atualiza os dados dos 4 produtos com conteúdo real, não inventado.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: Nenhum forte
**Complexity**: Medium — 4 páginas HTML + JS de renderização + migração de schema de dados já em produção

### Specialized Agent Assignment

**Primary Agents**:
- @dev (implementa)

**Supporting Agents**:
- @architect (quality gate — validar a migração de schema)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa
- [ ] Pre-PR (@devops): Run `coderabbit --prompt-only --base main` antes do push

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Migração de schema não quebra a Story 1.3 (catálogo) — `js/main.js` precisa ser atualizado junto
- Acessibilidade: `alt` por imagem da galeria, heading hierarchy (1 `H1` por página de produto)

**Secondary Focus**:
- Responsividade mobile-first
- Nenhum dado de produto hardcoded no HTML — tudo vem de `products.json`

## Tasks / Subtasks

- [x] Task 1: Estender o schema de `data/products.json` e popular com dados reais (AC: 2)
  - [x] Adicionar campo `description` a cada um dos 4 produtos, com base na inspeção direta das fotos reais (não inventar):
    - `camiseta-marrom-simples`: "Camiseta oversized na cor marrom, com o logotipo LAMIT estampado no peito em branco."
    - `camiseta-brasao-preta`: "Camiseta oversized preta, com o brasão da LAMIT estampado no peito e 'LAMIT' grafado nas mangas em laranja, estilo escrita à mão."
    - `camiseta-salvador`: "Camiseta com o logotipo LAMIT no peito e uma ilustração no verso inspirada em Salvador — um farol, coqueiros, barcos e o pôr do sol. Disponível em 3 variações: Azul (estampa navy/laranja em camiseta branca), Dourada (estampa dourada em camiseta branca) e Preta (estampa branca em camiseta preta)."
    - `ecobag`: "Descrição em breve." (sem foto nem dados de variante ainda — não inventar características)
  - [x] Migrar o campo `image` (string) para `images` (array de strings) em todos os produtos:
    - `camiseta-marrom-simples`: `images: ["/assets/products/camisetas/4 - camisaLamitMarromSimples.png"]`
    - `camiseta-brasao-preta`: `images: ["/assets/products/camisetas/5 - camisaLamitBrasãoPreta.png"]`
    - `camiseta-salvador`: `images: ["/assets/products/camisetas/6 - camisaLamitSalvadorAzul.png", "/assets/products/camisetas/6 - camisaLamitSalvadorDourada.png", "/assets/products/camisetas/6 - camisaLamitSalvadorPreta.png"]` — **contrato de dados:** `images[i]` corresponde a `colors[i]` na mesma posição
    - `ecobag`: `images: []`
  - [x] Atualizar `js/main.js` (Story 1.3) para usar `product.images[0]` em vez de `product.image` — sem isso, o catálogo quebra
- [x] Task 2: Criar as 4 páginas de detalhe (AC: 1)
  - [x] `produtos/camiseta-marrom-simples.html`, `produtos/camiseta-brasao-preta.html`, `produtos/camiseta-salvador.html`, `produtos/ecobag.html` — estrutura **idêntica** nas 4 (header com logo, breadcrumb vazio a preencher via JS, container `#product-detail` vazio, footer), conforme [Source: architecture/routing.md]
  - [x] Nenhum dado de produto hardcoded nas páginas — tudo populado por `js/product-detail.js`
  - [x] Footer marcado com o mesmo comentário `<!-- SHARED: footer v1 -->` já usado no catálogo [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 3]
- [x] Task 3: Criar `js/product-detail.js` (AC: 2, 3, 4)
  - [x] Deriva o ID do produto a partir do nome do arquivo na URL (`location.pathname`) — nenhuma página tem o ID hardcoded, mantendo as 4 páginas HTML idênticas
  - [x] `fetch('/data/products.json')`, encontra o produto pelo `id`
  - [x] Renderiza: breadcrumb ("Catálogo > {nome}"), galeria de fotos (uma `<img>` por item de `images`, com `alt` = nome do produto + posição se houver mais de uma) ou placeholder textual se `images` vazio (mesmo padrão da Story 1.3), nome (`h1`), descrição, lista de tamanhos (só renderiza se `sizes.length > 0`), lista de cores (só renderiza se `colors.length > 0`)
- [x] Task 4: CSS da página de detalhe (AC: 3, 4)
  - [x] Adicionar em `css/components.css`: estilos de breadcrumb, galeria (grid simples de miniaturas), lista de tamanhos/cores — reusando `var(--color-*)`, `var(--space-*)` já definidos (Story 1.2), nenhuma cor hardcoded
  - [x] Responsivo: empilhado no mobile, mais espaço/colunas no desktop, mesmos breakpoints do catálogo (600px, 1024px) [Source: architecture/styling-guidelines.md]
- [x] Task 5: Validação manual (AC: 1-4)
  - [x] Rodado localmente, testadas as 4 páginas de produto individualmente
  - [x] Confirmado via execução real (jsdom contra o servidor local): 4/4 páginas OK — produto certo, galeria de 3 imagens na Camiseta Salvador, placeholder na ecobag, 1 H1 por página
  - [x] Confirmado que o catálogo (Story 1.3) continua funcionando após a migração de `image` pra `images` (4 cards, imagens corretas, placeholder da ecobag preservado)
  - [x] Sem erros — scripts jsdom rodaram sem exceções em nenhuma das 5 páginas testadas

## Dev Notes

**Previous Story Insights:**
- Story 1.2: `.card`, `.btn`, tokens de cor/espaçamento/tipografia já existem em `tokens.css`/`components.css` — reusar.
- Story 1.3: `products.json` criado com schema `image` (singular) — **esta story migra esse campo pra `images` (array)**, e `js/main.js` precisa ser atualizado junto pra não quebrar o catálogo. Método de verificação via jsdom (fetch real + DOM real contra servidor local) validado e funcional — reusar a mesma abordagem. [Source: stories/1.3.pagina-catalogo-4-produtos.md]

**Data Models:**
Schema estendido de `data/products.json` (baseado em [Source: architecture/api-integration.md#estrutura-de-dados-dataproductsjson], com as extensões desta story):
```json
{
  "id": "string",
  "name": "string",
  "category": "string",
  "sizes": ["string"],
  "colors": ["string"],
  "images": ["string"],
  "description": "string",
  "price": "number|null",
  "deliveryNote": "string",
  "formsUrl": "string"
}
```
`images[i]` corresponde a `colors[i]` quando ambos existem (mesma posição).

**API Specifications:** N/A — sem API própria.

**Component Specifications:** Nenhum template de "página de detalhe" documentado previamente na arquitetura — esta story o define pela primeira vez, seguindo os Key Elements já listados em [Source: front-end-spec.md#pgina-de-detalhe-de-produto]: breadcrumb, galeria, nome/descrição/tamanhos/cores. Nota de prazo de entrega e CTA "Pedir" ficam **fora** do escopo desta story — são as Stories 2.2 e 2.3, respectivamente.

**File Locations:**
- `produtos/camiseta-marrom-simples.html`, `produtos/camiseta-brasao-preta.html`, `produtos/camiseta-salvador.html`, `produtos/ecobag.html` (novos — pastas já existem desde a Story 1.1)
- `js/product-detail.js` (novo)
- `js/main.js` (editado — migração `image` → `images[0]`)
- `data/products.json` (editado — schema estendido)
- `css/components.css` (editado — estilos de breadcrumb/galeria/variantes)

**Technical Constraints:**
- Nenhuma página de produto tem o ID do produto hardcoded — deriva de `location.pathname`, mantendo as 4 páginas verdadeiramente idênticas em conteúdo (só o nome do arquivo muda) [decisão desta story, documentada aqui porque não havia precedente anterior].
- Nunca hardcode dados de produto no HTML [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 2].
- Toda imagem exige `alt` descritivo [Source: architecture/frontend-developer-standards.md#critical-coding-rules, item 4].
- Migração de schema (`image` → `images`) deve atualizar `js/main.js` na mesma story — não deixar o catálogo quebrado entre stories.

**Project Structure Notes:** primeira story a criar arquivos dentro de `produtos/` (pasta existia vazia desde a Story 1.1) e a criar um segundo arquivo JS (`product-detail.js`) além de `main.js`.

### Testing

- **Test file location:** N/A — sem testes automatizados.
- **Test standards:** validação manual conforme checklist, incluindo execução real via jsdom (não só análise estática) — padrão estabelecido pelo QA na Story 1.3. [Source: architecture/testing-requirements.md]
- **Testing frameworks:** nenhum framework de teste; jsdom usado como ferramenta de verificação pontual, fora do projeto (scratch), não como dependência do site.
- **Requisitos específicos desta story:** as 4 páginas renderizam o produto correto; galeria da Camiseta Salvador mostra as 3 imagens; ecobag mostra placeholder; tamanhos/cores omitidos quando vazios (não aparecem como lista vazia); catálogo (Story 1.3) continua funcionando após a migração do schema.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-13 | 0.1.0 | Story criada a partir de `prd/epic-2-paginas-de-produto-encaminhamento-de-pedido.md`, com extensão de schema (description, images) identificada e escopada nesta story | River (@sm) |
| 2026-08-13 | 0.2.0 | Todas as tasks implementadas, incluindo migração de schema e verificação real via jsdom nas 5 páginas. Status: Draft → Review | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (@dev persona "Dex", via AIOX)

### Debug Log References

- `node --check` em `js/product-detail.js` e `js/main.js`: sintaxe OK.
- `data/products.json`: JSON válido após a migração de schema.
- Verificação real via jsdom (servidor local): 4/4 páginas de produto OK (produto certo, contagem de imagens certa, placeholder certo, 1 H1 cada); catálogo (Story 1.3) confirmado intacto após a migração `image` → `images`.

### Completion Notes List

- Schema de `products.json` estendido com `description` e `images` (migrado de `image` singular). Descrições escritas com base na inspeção direta das 4 fotos de produto — a Camiseta Salvador realmente tem 3 variações distintas de cor de camiseta/estampa (não a mesma imagem repetida), confirmado visualmente antes de escrever a descrição.
- `js/main.js` (Story 1.3) atualizado pra usar `images[0]` — migração feita na mesma story pra não deixar o catálogo quebrado entre stories, conforme alertado pelo `@sm`.
- As 4 páginas de produto são **idênticas** em conteúdo — o ID do produto vem só do nome do arquivo (`location.pathname`), nunca hardcoded. Isso significa que criar um 5º produto no futuro (drop futuro) só vai exigir copiar o HTML e adicionar a entrada em `products.json`, sem tocar em JS.
- Tamanhos/cores só renderizam quando o array não está vazio — a ecobag (sem essas informações ainda) não mostra listas vazias/quebradas.
- Nenhuma dependência nova.

### File List

- `data/products.json` (modificado — schema estendido)
- `js/main.js` (modificado — migração pra `images[0]`)
- `js/product-detail.js` (criado)
- `produtos/camiseta-marrom-simples.html` (criado)
- `produtos/camiseta-brasao-preta.html` (criado)
- `produtos/camiseta-salvador.html` (criado)
- `produtos/ecobag.html` (criado)
- `css/components.css` (modificado — breadcrumb, galeria, variantes)

## QA Results

_A ser preenchido pelo @qa após a implementação._
